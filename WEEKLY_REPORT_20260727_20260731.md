# 周报（2026-07-27 至 2026-07-31）

## 本周工作概述

本周完成了 PI0.5 在 SO-101 抓取任务上的 VLASH 训练、异步推理链路实现与真机验证。当前模型
已经能够根据 top、wrist 双相机图像完成物体接近、夹取和搬运流程，推理与控制链路可以连续
运行；现阶段主要问题是动作速度偏慢，抓取准确率和跨场景泛化仍需继续提升。

## 训练工作

- 使用 `admin123/so101_test_data` 的 40 个 episode、17,960 帧数据进行训练，全部数据用于训练，
  未设置验证集。
- 在 PI0.5 基础权重上完成 10 个 epoch、5,613 step 的专家分支微调，batch size 为 32。
- 启用 `state_cond=true`、`temporal_offset_max_steps=8` 和 `train_expert_only=true`，使模型适配
  VLASH 的未来状态条件推理。
- 训练过程无 NaN、Inf、OOM 或梯度异常；后期 loss 均值约为 `0.01275`，最终记录 loss 为
  `0.012`。epoch 6 后下降趋缓，训练基本进入平台期。
- 最终权重位于：
  `outputs/train/pi05_so101_vlash_expert_only_10epochs_bs32_seed1000/checkpoints/005613/pretrained_model`。

## 推理链路与真机结果

- 新增 VLASH 异步固定 chunk 推理：当前采用 30 Hz、`execution_horizon=10`、
  `inference_overlap_steps=5` 和 `max_future_state_delta=5.0`。
- 在当前 chunk 剩余 5 步时，根据真实观测状态、实际下发动作、硬件 clamp 和近期关节跟踪增益，
  预测 chunk 边界的未来状态，并在后台生成下一完整 chunk。
- 最新 60 秒真机测试记录 1,778 次动作反馈，实际控制频率约 29.6 Hz；稳态推理 P95 为
  134.76 ms、最大 157.18 ms，低于约 166.67 ms 的 overlap 预算，deadline miss 为 0。
- 当前机械臂已经能够完成抓取，说明相机、模型、VLASH、动作后处理和机械臂控制已形成有效闭环。

## RTC 与 VLASH 对比结论

RTC 和 VLASH 是两种独立推理模式，当前真机只使用 VLASH，RTC 没有同时参与。RTC 通过旧动作
prefix guidance、实际消耗步数计算和新旧 chunk 融合来补偿推理延迟；VLASH 则预测当前 chunk
结束时的未来机械臂状态，在边界处整块切换到提前生成的新 chunk。当前权重经过状态条件和
temporal offset 训练，与 VLASH 的匹配度更高；后续可将 RTC 作为对照方案测试动态纠偏能力。

## 当前问题与后续计划

- 速度偏慢不是控制频率不足导致：控制循环已接近 30 Hz，且没有推理 deadline miss。
- 动作滤波器改写约占 98.3%，硬件 clamp 约占 62.1%，平均关节跟踪增益约为 0.180，这些因素
  是当前运动速度和轨迹滞后的主要限制。
- 40 个 episode 全部用于训练，没有独立验证集，因此现有结果不能充分说明新物体位置、光照和
  初始姿态下的泛化能力。
- 下一步优先让动作滤波器持续使用真实 observation/sent action 校正内部状态，再进行小步位置环
  参数测试；同时补充节奏更快、停顿更少的成功示教，并开展 RTC/VLASH 同条件对照测试。
