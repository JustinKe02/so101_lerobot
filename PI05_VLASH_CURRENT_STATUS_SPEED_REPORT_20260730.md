# PI0.5 + VLASH 当前版本总结与抓取速度分析

日期：2026-07-30

## 1. 结论摘要

当前版本已经能够完成真实机械臂抓取。根据现场观察，机械臂已能够接近物体、闭合夹爪并抓住
物体，说明图像输入、PI0.5 权重、动作块生成、VLASH 异步推理和真实机械臂控制已经形成有效
闭环。

当前抓取速度慢，主要不是控制频率不足，也不是 GPU 推理等待造成的：

- 数据集、相机和控制循环均为 30 Hz。
- 最新 60 秒真机运行最后记录 1,778 个动作反馈，折合全程约 29.6 Hz；扣除首次模型推理
  约 385 ms 后，有效控制频率约 29.8 Hz。
- 日志没有 `Record loop is running slower` 告警。
- steady-state 推理 P95 为 134.76 ms，最大为 157.18 ms，低于 overlap 5 在 30 Hz 下
  约 166.67 ms 的 deadline 预算。
- 全程没有 deadline miss、ERROR、CRITICAL 或 traceback。

真正限制速度的是：动作滤波器几乎持续介入、策略绝对目标长期领先于实际机械臂位置、硬件
clamp 频繁发生，以及电机对位置目标的实测跟踪增益较低。抓取速度可以提高，但不应直接通过
提高 FPS 或取消 `max_relative_target=5.0` 实现。

建议保持当前 30 Hz、overlap 5 和 `max_relative_target=5.0`，按以下顺序提速：

1. 让动作输出滤波器使用实际 observation/sent action 校正内部状态。
2. 在短时受控测试中将位置环 P 系数从 16 小步提高到 20，再视结果测试 24。
3. 电机跟踪改善后，再对 shoulder lift、elbow 和 gripper 的滤波速度/加速度上限提高约 10%。
4. 记录或补充节奏更快、停顿更少的成功示教，再训练一版速度更快的策略。

## 2. 当前训练与模型

### 2.1 数据集

- 数据集：`admin123/so101_test_data`
- Episode：40
- 总帧数：17,960
- 频率：30 Hz
- 相机：top、wrist
- 状态/动作维度：6/6
- 动作定义：绝对关节目标
- 任务：`Put the block in the bin`
- 数据划分：40 个 episode 全部训练，没有独立验证集

### 2.2 最终权重

最终 checkpoint：

`outputs/train/pi05_so101_vlash_expert_only_10epochs_bs32_seed1000/checkpoints/005613/pretrained_model`

训练配置：

- 10 epoch，5,613 steps，batch size 32
- `state_cond=true`
- `temporal_offset_max_steps=8`
- `train_expert_only=true`
- 最后一个 epoch 平均 loss：0.012772
- 最终记录 loss：0.012

训练在 epoch 6 以后进入平台区。继续单纯增加 epoch 不太可能明显提高速度，因为行为克隆策略
还会复现示教数据本身的运动节奏和停顿。

## 3. 当前版本完成的工作

### 3.1 PI0.5 状态条件

PI0.5 增加机械臂状态条件输入。模型不再只根据图像和任务文本生成动作，也能够根据当前或
VLASH 预测的未来关节状态生成下一动作块。

### 3.2 Temporal offset 训练

训练数据处理支持最大 8 步 temporal offset，使模型在训练阶段接触异步部署中可能出现的
未来状态条件。当前部署投影 6 步，仍在训练覆盖范围内。

### 3.3 异步固定动作块推理

当前参数：

- `execution_horizon=10`
- `inference_overlap_steps=5`
- `max_future_state_delta=5.0`
- 控制频率 30 Hz
- deadline miss limit 1

机械臂执行当前 10 步 chunk 时，VLASH 在剩余 5 步时异步生成下一 chunk。推理和动作执行重叠，
因此约 120-135 ms 的模型推理不会直接表现为机械臂停顿。

### 3.4 真实动作反馈外推

每次动作经过输出滤波器和机器人安全 clamp 后，VLASH 接收：

- 原始策略动作
- 滤波后的 requested action
- 机器人实际发送的 sent action
- 当前实测关节状态

VLASH 使用最近 8 个周期估算每个关节的跟踪增益，并据此预测下一 chunk 边界的实际状态。
这减少了“模型认为机械臂已经到位、实际机械臂仍在追赶”的状态漂移，改善了接近、夹爪闭合和
抬起阶段的时序一致性，是当前能够稳定抓取的重要因素之一。

### 3.5 相机与运行保护

- 全部相机连接后额外预热 5 秒，使自动曝光在首个策略观察前稳定。
- 保留 `max_relative_target=5.0`。
- 保留二阶动作输出滤波器。
- 保留 VLASH deadline fail-closed。
- `stall_guard_ticks=0`，避免数据中正常的 action/state lag 被旧 stall guard 误判为堵转。
- launcher 支持 60 秒和 `duration=0` 不限时运行。

## 4. 最新 60 秒真机结果

日志：

`outputs/rollout/vlash_epoch10/vlash_60s_feedback.log`

运行时间：2026-07-30 21:03:20 至 21:04:20。

### 4.1 推理与控制

| 指标 | 结果 |
|---|---:|
| 目标控制频率 | 30 Hz |
| 最后记录的动作反馈数 | 1,778 |
| 全程折算反馈频率 | 约 29.6 Hz |
| 扣除首次推理后的有效频率 | 约 29.8 Hz |
| VLASH 推理次数 | 179 |
| steady-state 推理次数 | 178 |
| steady-state 平均时延 | 122.88 ms |
| steady-state P95 | 134.76 ms |
| steady-state 最大时延 | 157.18 ms |
| 首次推理时延 | 384.98 ms |
| Deadline miss | 0 |
| 控制周期过慢告警 | 0 |

### 4.2 动作执行

| 指标 | 结果 |
|---|---:|
| Hardware clamp 周期 | 1,104 / 1,778，约 62.1% |
| Action filter rewrite | 1,747 / 1,778，约 98.3% |
| 跟踪增益平均值 | 0.180 |
| 跟踪增益中位数 | 0.185 |
| 跟踪增益范围 | 0.000-0.365 |

关节 clamp 分布：

| 关节 | Clamp 次数 | 平均残差 | P95 残差 | 最大残差 |
|---|---:|---:|---:|---:|
| gripper | 757 | 8.443 | 20.351 | 21.817 |
| shoulder lift | 512 | 9.837 | 29.692 | 35.463 |
| elbow flex | 378 | 9.879 | 23.627 | 26.881 |
| shoulder pan | 144 | 3.841 | 10.442 | 11.500 |
| wrist flex | 28 | 0.550 | 1.174 | 1.224 |
| wrist roll | 1 | 0.028 | 0.028 | 0.028 |

同一 clamp 周期可能包含多个关节，所以关节次数之和大于 clamp 周期数。

## 5. 是否是控制频率导致速度慢

结论：不是。

### 5.1 当前控制频率已经达到目标

当前数据集、相机和控制循环都按 30 Hz 工作。实际反馈频率接近 30 Hz，且日志没有控制周期
overrun。动作控制线程没有因为 CPU、相机或串口而持续掉帧。

### 5.2 当前推理不会阻塞动作

VLASH steady-state P95 为 134.76 ms，推理发生在剩余 5 个控制步期间。30 Hz 下 5 步提供
约 166.67 ms，当前最大 157.18 ms 仍能按时完成。日志中 0 deadline miss 也验证了这一点。

### 5.3 直接提高 FPS 可能更差

如果保持 overlap 5：

- 30 Hz 的推理预算约为 166.67 ms。
- 40 Hz 的推理预算只有 125 ms，已经低于当前 P95 134.76 ms 和最大 157.18 ms。
- 60 Hz 的推理预算只有 83.33 ms，当前后端无法满足。

此外，相机是 30 Hz，直接将控制提高到 40/60 Hz 会重复使用相机帧；动作和 temporal offset
也是按 30 Hz 数据训练。动作滤波器的速度上限以“每秒”为单位，FPS 提高后每 tick 的允许位移
会相应减小，因此提高 FPS 并不会自然提高机械臂的物理关节速度。

如果未来确实需要 40/60 Hz，需要同时重新设计相机、数据重采样、temporal offset、overlap 和
推理后端，而不是只改 `--fps`。

## 6. 当前速度瓶颈

### 6.1 动作滤波器持续介入

二阶滤波器当前几乎每个控制周期都修改策略动作。30 Hz 下默认速度上限对应每 tick 最大命令
变化约为：

| 关节 | 速度上限 | 每 tick 最大变化 |
|---|---:|---:|
| shoulder pan | 66 units/s | 2.20 |
| shoulder lift | 95 units/s | 3.17 |
| elbow flex | 95 units/s | 3.17 |
| wrist flex | 53 units/s | 1.77 |
| wrist roll | 40 units/s | 1.33 |
| gripper | 102 units/s | 3.40 |

滤波器还限制加速度，所以每次启动、停止和反向都会经历速度爬升。shoulder lift 从零速度达到
当前上限理论上约需 0.30 秒，这种延迟在接近和抬起阶段会反复累积。

更重要的是，当前滤波器只在第一次调用时从实测 observation 初始化。之后内部位置 `_q` 按
滤波命令积分，没有用机器人实际 sent action 或下一帧 observation 持续校正。因此，当电机未能
跟上命令时，滤波器内部轨迹会逐渐领先于真实机械臂，随后由机器人 `max_relative_target=5.0`
持续 clamp。

### 6.2 电机实际跟踪偏慢

平均跟踪增益只有 0.180，表示从统计意义上看，下一周期实际状态只消化了前一周期命令误差的
一小部分。这个指标不是严格的电机传递函数，但足以说明真实关节响应明显慢于目标变化。

SO follower 当前把所有电机的 `P_Coefficient` 设置为 16，而电机默认值为 32。这个设置用于减少
抖动，但也会降低位置误差下的响应强度。底层电机 acceleration 已设置为 254，因此直接继续提高
acceleration register 不是当前首要方向。

gripper 还保留 50% 最大扭矩和保护电流设置，用于避免烧毁电机。不能为了速度直接取消这些保护。

### 6.3 模型复现示教节奏

当前是绝对关节目标的行为克隆策略。40 个 episode 中的接近速度、停顿、夹爪闭合时机和抬起节奏
都会成为模型学习目标。即使执行层完全跟踪，模型仍可能复现示教中的谨慎或慢速行为。

### 6.4 动作尾部误差仍然存在

最终 checkpoint 的快速检查仍有 gripper、shoulder lift 和 elbow flex 尾部误差。这些关节正是
真机 clamp 最集中的关节。VLASH 能修正下一 chunk 的状态条件，但不能重写当前 chunk 中已经生成
的远距离绝对目标。

## 7. 能否提高抓取速度

可以，但应先提高真实跟踪和命令一致性，再提高目标速度。当前版本已经能够抓取，激进地同时修改
多个参数会失去现有成功基线，也无法判断哪项修改真正有效。

### 阶段 1：动作滤波器闭环校正

保持以下参数不变：

- FPS 30
- overlap 5
- `max_future_state_delta=5.0`
- `max_relative_target=5.0`
- 当前滤波速度和加速度上限
- 电机 P 系数 16

修改动作滤波器，使其在每次机器人发送动作后，根据 sent action 和下一帧 observation 对内部
`_q`、`_v` 进行有限校正，避免滤波器轨迹长期领先真实机械臂。

该阶段的首要目标不是提高上限，而是把 clamp 周期从当前约 62% 降到 50% 以下，并确保抓取成功率
不下降。

### 阶段 2：小步提高位置环跟踪

完成阶段 1 后，在人工监护的短时测试中：

1. 将主要关节 `P_Coefficient` 从 16 调到 20。
2. 先运行 10-20 秒空载轨迹，再运行短时抓取。
3. 检查抖动、过冲、噪声、电流、温升和夹爪接触情况。
4. 如果稳定，再测试 24；不要直接跳到默认值 32。

预期判断指标：平均跟踪增益从约 0.18 提高到至少 0.23，且没有持续振荡或无关动作。

### 阶段 3：小幅提高关节滤波上限

只有在阶段 2 后 clamp 和跟踪明显改善时，才建议先进行一档约 10% 的调整：

| 关节 | 当前速度 | 第一档建议 | 当前加速度 | 第一档建议 |
|---|---:|---:|---:|---:|
| shoulder lift | 95 | 105 | 317 | 350 |
| elbow flex | 95 | 105 | 396 | 435 |
| gripper | 102 | 112 | 924 | 1,015 |

shoulder pan 和两个 wrist 关节先保持不变。每次只调整一档，并与当前日志做相同 60 秒对比。

如果在电机跟踪未改善前直接提高这些上限，滤波器只会更快地跑到真实机械臂前面，clamp 可能增加，
抓取时序反而会恶化。

### 阶段 4：补充更快的成功示教

执行层优化后，如果自由空间移动和动作阶段切换仍然慢，应补充 10-20 个节奏更快但成功率稳定的
episode：

- 减少接近前和抓取后的无意义停顿。
- 保持夹爪闭合时机一致。
- 保持相机、物体和初始姿态与当前部署一致。
- 留出独立验证 episode，不再全部用于训练。

重新采集比简单跳帧更可靠。直接跳过策略动作或对现有视频/状态/action 做不一致的时间压缩，可能
破坏接触时序。

## 8. 不建议的提速方式

- 不建议直接将 FPS 提高到 40/60。
- 不建议仅为了消除 warning 而提高或取消 `max_relative_target=5.0`。
- 不建议将 overlap 从 5 降到 4；当前 P95 已超过 4 步在 30 Hz 下约 133.33 ms 的预算。
- 不建议直接关闭动作滤波器。
- 不建议一次同时修改滤波上限、电机 P、FPS 和模型权重。
- 不建议提高 gripper 扭矩/保护电流来追求速度。

## 9. 提速实验验收标准

建议以当前 60 秒日志作为基线，每个阶段使用相同物体位置、初始姿态、seed 和任务文本，至少执行
10 次抓取，并记录：

1. 抓取成功率。
2. 从开始运动到夹爪稳定抓住物体的时间。
3. 完整任务周期时间。
4. clamp 周期比例和各关节残差。
5. filter rewrite 比例。
6. tracking gain。
7. 控制频率、推理 P95/最大值和 deadline miss。
8. 是否出现抖动、过冲、无关动作、异常噪声或明显温升。

第一轮提速目标建议设为：在抓取成功率不下降、deadline miss 为 0、控制频率保持 29 Hz 以上的
前提下，将中位抓取时间缩短 15%-25%。

## 10. 最终判断

当前版本能够抓取，说明 VLASH 状态反馈和时序修订方向有效。当前慢速不是因为 30 Hz 控制频率
不足，而是因为动作滤波器、硬件 clamp、较低的位置环响应和示教节奏共同限制了机械臂的实际运动。

最值得实施的下一项改动是“动作滤波器闭环校正”，随后再小步测试电机 P 系数。只有在真实机械臂
已经能够更好地跟随目标后，提高滤波速度上限或训练更快示教才会稳定转化为更快的抓取动作。
