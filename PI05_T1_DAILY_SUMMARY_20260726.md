# PI0.5 T1 真机监督运行 — 今日总结（2026-07-26）

> 路线约束（贯穿全程）：**改系统，不放宽门禁**。延迟门禁固定 166.667ms（= 30fps 重规划预算），
> 钳制门禁固定为 0。任何一次运行的失败都指向系统需要改进，而不是阈值需要放松。

## 1. 今日发生了什么（两次真机监督运行）

今天在用户到场监督、逐次授权下，执行了两次 T1（TensorRT prefix + 逐关节输出滤波 + Q45 固定引导 RTC）
真机运行。两次都在约 10 秒时中止，但**中止机制完全无关**。

| | 运行 #1（21:13） | 运行 #2（22:12） |
|---|---|---|
| 日志 | `outputs/rollout/t1_q45/t1_q45_20260726_211300.log` | `outputs/rollout/t1_q45/t1_q45_20260726_221255.log` |
| 中止机制 | 策略下压触桌 → 目标跑飞 → 撞击震掉腕相机 → 崩溃 | 单次 185.9ms 延迟尖峰撞破引导窗口 → 护栏 fail-closed 停机 |
| 物理后果 | 机械臂瘫在桌面（旧 teardown 松扭矩） | **机械臂定住保持姿态**（新 teardown 保扭矩，首次实战生效） |
| 延迟 p50 / p95 / max | 131.698 / 139.944 / **141.496** ✓ | 133.371 / 140.674 / **185.858** ✗ |
| 自由空间钳制 | 0（撞击后 26 次接触钳制 + 2 次启动瞬态 = 28） | **0**（仅 2 次启动瞬态） |
| actual_consumed max | 4 ✓ | 5 ✓ |
| 重规划 | ~6Hz ✓ | ~6Hz ✓ |
| 日志契约 token | 15/15 ✓ | 16/16 ✓（含新增 stall_guard_runtime） |

两次分析器都判 `passed=false`，判得对——门禁没有放宽。

## 2. 运行 #1 根因（撞桌事件）

四步因果链，每步都有日志证据：

1. **策略下压触桌**：在 5 秒以后从未离线验证过的轨迹区域，策略输出了一段包络内、滤波器
   限速内的快速下压。shoulder_lift 实际位置到达约 −99（标定量程下限 −100）——物理压到桌面。
2. **接触后目标跑飞**：关节被顶住走不动（每 tick 仅 ~0.9–1.9），但指令目标仍按每 tick +5
   前进，触发连续 26 次 `max_relative_target` 钳制（21:14:35–36）。
3. **冲击震掉腕相机**：/dev/video6 从 USB 总线消失（errno=19），读线程死亡，
   `get_observation` 抛错，rollout 崩溃。
4. **崩溃时松扭矩**：旧 teardown 按默认 `disable_torque_on_disconnect=True` 释放扭矩，
   臂瘫在桌面——即"砸到桌面"的最后一幕。

**关键区分**：28 次钳制 ≠ 滤波器失效。其中 2 次是无害启动瞬态，26 次是接触诱发的目标跟踪误差
钳制（滤波器无法阻止跟踪误差钳制）。滤波器成功消除了**所有自由空间钳制**（T0a 时代是 77 次）。

## 3. 运行 #2 根因（延迟尖峰 → 护栏停机）

**没有物理事故**。前 54 次重规划全部健康（p50 133.4ms / p95 140.7ms，全部 < 166.667 门禁），
第 55 次触发链：

1. **延迟尖峰**：该次推理耗时 185.858ms（≈5.6 tick），`wall_latency_steps=6`，
   超过固定引导延迟 5 tick 的覆盖范围，也超过延迟门禁。
2. **引导窗口零余量**：Q45 下，名义重规划间隔 5 tick + 固定延迟 5 tick = 10 = `execution_horizon`，
   **恰好顶格无余量**。尖峰把周期拉长到 6 tick，消费指针要取 `model_action_index=10`——已在窗外。
3. **护栏拒发**：`enforce_guided_execution_window=true`（T0a 时代定下的防跳变护栏）拒绝下发
   无引导约束的动作，引擎进入终止态，主循环停止。
4. **异常收尾按新语义执行**：跳过回初始位、扭矩保持、臂定住、相机干净断开。

## 4. 今天已实现并验证的软件防护（改系统）

在两次运行之间落地了三层防护，运行 #2 已实战验证其中两层：

- **失速/接触守护**（`src/lerobot/rollout/stall_guard.py`，新增）：逐 tick 比较策略请求与实际下发
  的动作，连续 5 tick（≈167ms）偏差超容差即判定堵转/意外接触 → 先命令保持当前位置，再抛错中止。
  五种 strategy 共用同一路径。运行 #2 全程零误触（仅启动 2 次瞬态微钳制，未达 5 tick 阈值）。
- **异常退出扭矩保持**（`strategies/core.py`）：运行报错时 teardown 跳过回初始位、保持扭矩、
  打印操作提示。运行 #2 首次实战生效——机械臂定住未塌。Ctrl-C 正常退出语义不变。
- **全链 fail-closed 日志契约**：新配置字段经启动脚本显式传参、preflight 进程内断言、配置回显；
  T1 启动器拒绝 `stall_guard_ticks=0`；分析器新增 `--expect-stall-guard true`——守护若被静默关闭，
  分析直接判不通过（与 TRT/滤波期望同一套契约）。

验证状态：58 项测试全过（新增 8 项守护测试），ruff 干净，无硬件离线预检三门禁全绿，
回显含 `stall_guard_ticks=5 / tolerance=0.001`。

## 5. 当前待解决的问题（下一步系统改进方向）

按优先级，**全部为无硬件离线工作**，可在不碰机械臂的前提下完成：

### A（主修）引导窗口零余量 → `execution_horizon` 10 → 12
- 现状：Q45 下窗口余量为 0 tick，任何一次超过 5 tick 的延迟尖峰都会触发护栏停机。
- 方案：`execution_horizon` 10→12，把尖峰容忍从 0 提到 2 tick（≈67ms）。
- 为什么不调 Q：Q46 会把重规划预算压到 133ms（恰好是 p50，一半会迟到）；Q44 让窗口更糟。调 Q 无解。
- 前置门禁：改 horizon 后需先离线重跑 RTC 回放门禁 + §8.2 real-frame parity，确认引导质量不退化。

### B（辅修）延迟尾部治理
- 185.9ms 尖峰与 T0a 时代的 191.5ms 同源（本机偶发尾部事件，55 次里出现 1 次）。
- 手段：锁 GPU 频率、控制循环内 GC 调优、排查后台负载。

### C（顺手修）启动瞬态钳制 → 消除，让 zero-clamp 门禁可真正全绿
- 根因：首个指令与臂实际起始位姿的小偏差。
- 方案：用当前观测位置播种滤波器首指令。

### D（待用户决定，需物理数据）工作空间软限位 / 关节软下限
- 软件守护只能在接触发生后 ~167ms 内止损，**无法阻止策略首次伸向桌面**（运行 #1 的根因）。
- 要从根上防，需要基于桌面实际高度的工作空间软限位。但单关节下限无法表达桌面平面，
  需要用户遥操作把机械臂放到桌面几个位置采集关节值。**此项需用户决定是否采集数据后再实现。**

## 6. 一个观察（不下结论）

两次都停在 ~10 秒，但机制完全无关（#1 策略下压触桌，#2 延迟尖峰）。策略在 10 秒附近正处于
抓取下压阶段。下次跑过这个点才算真正进入长时未验证区域——A/B/C 修完后值得关注这个时间窗。

## 7. 硬件状态

- 运行 #2 结束后机械臂通电保持姿态；用户已手动归位；随后执行去扭矩操作，
  **机械臂当前无力、可安全触碰**。
- 串口 `/dev/serial/by-id/usb-1a86_USB_Single_Serial_5A7C123192-if00` 与相机
  `/dev/video4`、`/dev/video6` 无残留进程占用。
- **腕相机 USB 连接器/支架**在运行 #1 被撞掉过，运行 #2 全程健康——但物理加固仍建议保留关注。

## 8. 下次真机运行的前置条件（缺一不可）

1. A/B/C 三项离线修复完成，且 RTC 回放门禁 + parity 全部绿灯。
2. 用户到场，逐次明确授权（授权不常设，agent 不得自行驱动机械臂）。
3. 运行前用 `fuser` 确认串口与两个相机空闲。
